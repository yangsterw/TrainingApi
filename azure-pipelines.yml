# Build and deploy Training API

trigger:
- master

resources:
- repo: self

variables:
  tag: 'v5'

stages:
- stage: Build
  jobs:  
  - job: Build
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: Docker@2
      displayName: 'Build and Push Docker Image: $(tag)'
      inputs:
        containerRegistry: 'Docker Hub Registry'
        repository: 'oitgaden/trainingapi'
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
        tags: $(tag)
- stage: Deploy
  jobs:
  - job: Deploy
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: KubernetesManifest@0
      displayName: 'Deploy Training API: $(tag)'
      inputs:
        action: 'deploy'
        kubernetesServiceConnection: 'Training API Cluster'
        namespace: 'default'
        manifests: 'kubernetes/deployment.yaml'
